# Custom Agents

This directory is the repo-local home for custom agents created during Codex-driven workflows.

Use it when:

- the user asks to create a new agent while working directly in this repository
- there is no active Claude runtime copy under `.claude/agents/`

When you add or remove a custom agent here, also update:

- `references/agents-registry.md`
- `references/agents.md`
